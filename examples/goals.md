# Demo Goals

- Scenario 1: find all places where `AssistantConfig.from_env` is used and summarize the results by runtime role.
- Scenario 2: update `README.md` from the current implementation and preview the diff before any write.
- Scenario 3: check that the README MCP tool list matches the actual tool registrations.

See `examples/demo_prompts.md` for the full prompts, commands, expected output sections, and safe review notes.
