# Demo Goals

- Summarize the authentication flow across `auth.py`, `config.py`, and `cli.py`.
- Search for duplicated logging setup across `cli.py`, `app.py`, and `worker.py`, then propose a cleanup and show a diff.
- Analyze how environment configuration flows through `settings.py`, `env.py`, and `main.py`.
- Propose a small documentation update based on `README.md`, `pyproject.toml`, and `src/project_assistant/cli.py`.

